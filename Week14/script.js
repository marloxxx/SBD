// script menjawab kusioner secara otomatis

// Sangat baik
var numKusioner = 68;
for(let i = -1; i < numKusioner; i++){
    $('.radio-inline').eq(4 + (4 * i)).click();
}

// Baik
var numKusioner = 68;
for(let i = -1; i < numKusioner; i++){
    $('.radio-inline').eq(5 + (4 * i)).click();
}

// Cukup
var numKusioner = 68;
for(let i = -1; i < numKusioner; i++){
    $('.radio-inline').eq(6 + (4 * i)).click();
}

// Kurang
var numKusioner = 68;
for(let i = -1; i < numKusioner; i++){
    $('.radio-inline').eq(7 + (4 * i)).click();
}